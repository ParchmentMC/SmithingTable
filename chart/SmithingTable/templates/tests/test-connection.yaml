apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "SmithingTable.fullname" . }}-test-connection"
  labels:
    {{- include "SmithingTable.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "SmithingTable.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
